# Data

Do not commit downloaded datasets, recordings, or generated feature dumps to this repository.

Use the directories below for local development:

```text
data/
├── raw/         # source images/videos
└── processed/   # cleaned/normalized features
```

Dataset provenance and licensing belongs in `docs/dataset.md`.
